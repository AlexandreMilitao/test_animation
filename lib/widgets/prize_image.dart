import 'package:flutter/material.dart';

class PrizeImage extends StatelessWidget {
  const PrizeImage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Image.asset('assets/images/all_sequence/sequence__257.png'),
    );
  }
}
